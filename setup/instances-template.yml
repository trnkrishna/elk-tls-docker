instances:
  - name: elasticsearch
    dns:
      - elasticsearch
      - localhost
    ip:
      - HOST_IP
  - name: kibana
    dns:
      - kibana
      - localhost
    ip:
      - HOST_IP
  - name: logstash
    dns:
      - logstash
      - localhost
    ip:
      - HOST_IP
  - name: filebeat
    dns:
      - filebeat
      - localhost
    ip:
      - HOST_IP
  - name: metricbeat
    dns:
      - metricbeat
      - localhost
    ip:
      - HOST_IP
  - name: packetbeat
    dns:
      - packetbeat
      - localhost
    ip:
      - HOST_IP
